[package]
name = "tpex-normalizer"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
market-types = { path = "../../market-types" }
serde = { version = "1.0", features = ["derive"] }
serde_json = { version = "1.0", features = ["raw_value"] }

[lints]
workspace = true
