[package]
name = "replay-engine"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
blake3 = "1.8"
market-state = { path = "../market-state" }
market-types = { path = "../market-types" }

[[bench]]
name = "canonical_hot_path"
harness = false

[[bench]]
name = "multi_stream_merge"
harness = false

[lints]
workspace = true
